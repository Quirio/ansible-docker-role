[defaults]
inventory = inventory/gcloud
remote_user = alejandro
private_key_file = ~/.ssh/id_rsa
host_key_checking = false 
pipelining = true

[privilege_escalation]
become = true
become_method = sudo
become_user = root
become_ask_pass=true
